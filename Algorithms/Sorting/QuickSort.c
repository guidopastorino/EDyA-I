/*
Pasos del Algoritmo QuickSort
 - Seleccionar un pivote: Escoge un elemento del array como pivote. Existen varias estrategias para escoger el pivote, como elegir el primer elemento, el último elemento, el elemento medio, o un elemento aleatorio.
 - Particionar: Reorganiza los elementos del array de manera que todos los elementos menores que el pivote estén a la izquierda del pivote y todos los elementos mayores estén a la derecha. Después de esta partición, el pivote está en su posición final.
 - Ordenar recursivamente: Aplica los pasos 1 y 2 recursivamente a los subarrays de los elementos menores y mayores que el pivote.
*/

#include "Materials.h"

void quicksort(int* arr, int low, int high){
  if(low < high){
    
  }
}

int main(){

  return 0;
}